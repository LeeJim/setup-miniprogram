import { MiniProgramCI } from '../types';
export declare class TaskStatus implements MiniProgramCI.ITaskStatus {
    private _id;
    private _status;
    private _msg;
    constructor(msg: string);
    get id(): string;
    get status(): "done" | "doing";
    get message(): string;
    done(): void;
    toString(): string;
}
