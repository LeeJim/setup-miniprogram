import { ReactiveProject } from '../reactiveCache';
import { ExtJSON, MiniProgramCI } from '../../../types';
export declare const getExtJSON: (project: (MiniProgramCI.IProject & {
    onFileChange?: any;
}) | ReactiveProject) => ExtJSON.IExtJSON | undefined;
