import { ReactiveProject } from '../reactiveCache';
import { AppJSON, MiniProgramCI } from '../../../types';
export declare const getAppJSON: (project: (MiniProgramCI.IProject & {
    onFileChange?: any;
}) | ReactiveProject) => AppJSON.IAppJSON;
