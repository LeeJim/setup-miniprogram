import { ReactiveProject } from './reactiveCache';
import { IProject, ProjectConfigJSON } from '../../types';
export declare const getProjectConfigJSON: (project: ReactiveProject | IProject) => ProjectConfigJSON.IProjectConfigJSON;
