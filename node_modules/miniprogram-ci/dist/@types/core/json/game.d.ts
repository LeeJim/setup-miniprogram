import { ReactiveProject } from './reactiveCache';
import { IGameJSON, MiniProgramCI } from '../../types';
declare const getGameJSON: (project: (MiniProgramCI.IProject & {
    onFileChange?: any;
}) | ReactiveProject) => IGameJSON;
export default getGameJSON;
