import { ReactiveProject } from './reactiveCache';
import { SiteMapJSON } from '../../types';
export declare const getSiteMapJSON: (project: (import("../../types").MiniProgramCI.IProject & {
    onFileChange?: any;
}) | ReactiveProject) => SiteMapJSON.ISiteMapJSON;
