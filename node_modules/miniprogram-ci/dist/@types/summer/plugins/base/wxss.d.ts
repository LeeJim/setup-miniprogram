import { SummerPlugin } from '../../types';
export interface IWxssOptions {
    autoPrefix: boolean;
    minify: boolean;
}
export default function (projectPath: string, options?: IWxssOptions): SummerPlugin;
