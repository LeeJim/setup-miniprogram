import { MiniProgramCI } from '../types';
export interface IUploadOptions {
    project: MiniProgramCI.IProject;
    env: string;
    path: string;
    remotePath: string;
    concurrency?: number;
}
export declare function uploadFiles(options: IUploadOptions, mode: 'staticstorage' | 'storage'): Promise<void>;
